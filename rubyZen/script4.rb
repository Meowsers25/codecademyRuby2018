# ternary conditional expression. It's called
# "ternary" because it takes three arguments:
# a boolean, an expression to evaluate if the
# boolean is true, and an expression to evaluate
# if the boolean is false.

puts stinks = false ? "Luna stinks" : "No she doesn't"
